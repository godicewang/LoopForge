# LoopForge Forensic Redesign — Working Notes

## Non-negotiable boundary

- The EasyBusiness Graph task `1ED2180F-FBE1-4BCA-9E79-701F40CC3483` is permanently stopped.
- EasyBusiness is read-only evidence. No Loop, Graph, agent, manual edit, Git integration, revert, stash, commit, or push is permitted there during this investigation.
- Writes are limited to LoopForge while unrelated pre-existing changes are preserved.
- Completion requires at least 72,000 verified active seconds; the superseded 10-hour ledger contributes zero.

## 2026-08-16 productive-provider architecture scope retirement

- “Install a productive backend and cut over the native launcher” is now a
  retired ordinary-app strategy, not an implementation backlog item. The
  signed package, manifest, self-test, harness behavior, loader, readiness
  compiler, and receipt replay all authorize only `transportVetoOnly`.
- The semantic readiness blocker is
  `productiveProviderArchitectureUnavailable`; its schema-v1 raw value remains
  `providerHarnessProductiveBackendUnavailable`. Diagnostics state that binary
  installation or relabeling cannot replace separately isolated and ratified
  architecture.
- An adversarial manifest used the exact executable identity, productive mode,
  a Codex backend declaration, and a self-consistent productive self-test.
  Native selection still rejected it and minted no authority. Focused suites
  passed 23/0; complete and package-owned suites passed 868/8/0.
- Snapshot `4ec4c6aba1c46410eb322909657b84d3a68797e00e451f7575a7caee4af0fd44`
  passed Release, signing, checksums, direct and mounted startup, all-three-
  Mach-O identity, detach, and cleanup. A newly enrolled inert ready run showed
  the corrected veto with activation disabled and no attempt admitted; native
  screenshot SHA-256 is
  `664944cc600d13cde571e1b2f3b5bf3a217a450938fd8a75de4a6c36c9d14d9e`.
- A future productive route requires a separately authorized privileged or
  virtualized isolation product. The current ordinary-app release work is the
  complete native matrix, clean-revision rebuild, commit, and push. Final
  release remains false; EasyBusiness stayed read-only.
- The strict ledger accepts 600 seconds for this architecture decision,
  implementation, adversarial proof, exact packaging, native inspection,
  screenshot, and report work, bringing the total to 196,350 seconds across
  259 intervals. Rejected source-capture attempts, builds, tests, packaging,
  polling, and time beyond the interval cap count zero.

## 2026-08-16 ordinary-macOS physical-footprint strategy retirement

- The in-process ordinary-macOS Release issuer is now a disproven and retired
  strategy, not a generic pending implementation. The installed SDK defines
  `RLIMIT_RSS` as the `RLIMIT_AS` compatibility alias, and the exact
  `task_set_phys_footprint_limit` self probe returned `KERN_NO_ACCESS`.
  Address-space/data limits, previously disproven jetsam attributes for this
  process class, and parent sampling do not satisfy the hard pre-exec physical-
  footprint contract.
- Production external-observer, postimage-verifier, and contract-wide
  resolvers are no longer optional. They return exact non-serializable
  authority or the typed
  `ordinaryMacOSHasNoHardPreExecPhysicalFootprintLimit` terminal decision.
  Default production returns the veto; a future privileged/virtualized route
  must explicitly return exact authority.
- Durable raw values remain schema-v1 compatible. Focused suites passed 44/0
  and 5/0. Exact snapshot `b941070c46a9547eed6233bc891a8a52756556b84a7952ab3fb0949d9c9fa549`
  passed package-owned 868/8/0, Release, signing, checksums, direct and mounted
  startup, all-three-Mach-O identity, detach, and cleanup.
- Strict ledger interval `ordinary-macos-resident-memory-strategy-retirement-258`
  adds 600 accepted active seconds, bringing the exact total to 195,750 across
  258 intervals. The expected no-access probe, failed compile, builds/tests,
  waits, polling, and time beyond the cap count zero.
- Productive provider backend ratification/native launch cutover, the complete
  native trait/window/baseline/candidate matrix, clean-revision rebuild,
  commit, and push remain. Final release is false; EasyBusiness stayed
  read-only.

## 2026-08-15 production external-dependency controller composition

- `KernelProductionExecutionSession` now owns one complete external-dependency
  controller method: exact contract activation, activation-bound containment
  resolution, pre-admission veto or native launch, natural release, retained-
  output parse/mapping, release-bound result, and journal-owned observation.
- The default Release resolver is nil. The production-path test journaled
  `residentMemoryEnforcementUnavailable` and created no admission, lease,
  sandbox, process, runtime launch, result, or observation.
- With the DEBUG-only capability, the same production session used the real
  sandbox gate and raw-Darwin fixture to complete the native chain. Exact retry
  replayed the retained completion; a substituted launch ID rejected with no
  journal advance. No partial live process handle escapes the controller.
- External-observer launch now trusts the exact non-Codable readiness capability
  already bound to activation/journal/request/actor/attempt state, eliminating
  an impossible duplicate construction-time capability. Postimage-verifier
  containment remains unchanged.
- Forty production-enrollment tests passed. The exact source suite passed 811
  tests with 8 intentional skips and zero failures in 60.075 test seconds.
  Package-owned 811/8/0 passed in 59.388 test seconds; Release built in 101.53
  seconds; signing, archives, hashes, startup, cleanup, thermal, and read-only
  EasyBusiness checks passed.
- Strict ledger interval
  `production-external-dependency-controller-composition-222` adds 600 accepted
  active seconds, bringing the exact total to 174,810 across 222 intervals.
  Two incorrect packaging shell invocations and all failed work count zero.
- This closes production-session composition, not native app start/cutover.
  Ordinary-macOS Release still retains the containment veto; native start,
  final-completion invocation, legacy retirement, unlocked proof, clean rebuild,
  commit, and push remain.

## 2026-08-15 complete native external-dependency observation composition

- `JournaledProcessRuntime` now composes the full native observer chain without
  caller-supplied output, timeout, parse, mapping, result, release, or
  observation facts. It re-resolves exact activation/probe/launch/binding/lease
  authority, derives the deadline, naturally joins and journals release, reads
  bounded retained output descriptor-relatively, strictly parses and maps it,
  issues the release-bound result, and appends the journal-owned observation.
- Exact prior-release recovery, same-command retry, and replay through a fresh
  `RunJournal` resolve to the same durable result and observation without
  recreating native process authority. Unrelated releases and noncanonical
  native output reject; the latter appends no observation.
- A real sandboxed raw-Darwin fixture consumed the immutable canonical request,
  emitted canonical sorted output, exited naturally, completed the entire
  release/result/observation chain, and left zero runtime handles, in-doubt
  resources, or supervisor leases.
- The first diagnostic fixture used Foundation, which introduced
  `__CF_USER_TEXT_ENCODING` and correctly failed the exact-environment test.
  Restoring raw-Darwin parsing fixed it; the failed diagnostic/full run counts
  zero.
- The exact source suite passed 809 tests with 8 intentional skips and zero
  failures in 67.141 test seconds. Package-owned 809/8/0 passed in 63.748 test
  seconds; Release built in 96.47 seconds; signing, archives, hashes, startup,
  cleanup, thermal, and read-only EasyBusiness checks passed.
- Strict ledger interval
  `external-dependency-native-observation-composition-221` adds 600 accepted
  active seconds, bringing the exact total to 174,210 across 221 intervals.
  Failed diagnostics, builds, tests, waits, polling, and report time count zero.
- The proof still uses the DEBUG-only resident-memory capability. Ordinary-macOS
  Release retains the exact containment veto; production-controller invocation,
  final-completion invocation, legacy retirement, unlocked proof, clean rebuild,
  commit, and push remain.

## 2026-08-15 activation-bound external-dependency native sandbox spawn

- `JournaledProcessRuntime` now turns one live external-observer readiness
  capability into journal-first admission and the exact derived executable,
  request, minimal environment, read-only/offline sandbox, private I/O, kernel
  ceilings, wall deadline, and resource reservation. The caller cannot widen
  those launch facts.
- Request and staged-executable identity are revalidated before and after
  admission. The typed adapter route rehashes the exact open stdin descriptor;
  the runtime accepts identity only after the stopped sandbox gate and target
  exec handshake, then atomically journals process binding plus launch.
- A real native raw-Darwin fixture consumed the immutable request from stdin,
  wrote exact retained output, exited naturally, and released with zero runtime
  handles and supervisor leases. This actual spawn used a DEBUG-only synthetic
  resident-memory capability; no production observer is claimed.
- The integration exposed two concrete identity defects: Foundation retained
  `/var` while the native boundary reported `/private/var`, and stderr was
  compared in the stdout digest domain. Durable activation/I/O now use shared
  `F_GETPATH` physical directory identity, and stderr has a distinct digest.
- Fifteen focused tests passed. The exact source suite passed 808 tests with 8
  intentional skips and zero failures in 57.480 test seconds. Package-owned
  808/8/0 passed in 61.394 test seconds; Release built in 104.23 seconds;
  signing, archives, hashes, startup, cleanup, thermal, and EasyBusiness
  read-only checks passed.
- Strict ledger interval `external-dependency-native-sandbox-spawn-220` adds
  600 accepted active seconds, bringing the exact total to 173,610 across 220
  intervals. Failed diagnostics, builds, tests, waits, polling, and report time
  count zero.
- Ordinary-macOS Release still lacks a trusted hard resident-memory issuer and
  retains the typed pre-admission veto. Production parse/result/observation
  composition, controller invocation, legacy retirement, unlocked proof,
  clean rebuild, commit, and push remain.

## 2026-08-15 activation-bound external-dependency runtime launch journal

- A new durable runtime-launch receipt binds one exact activation and journal
  frame to the admitted process-tree lease, accepted binding, immutable request,
  staged executable, argv/environment, private I/O, sandbox attestation,
  parser/mappings, network/resource limits, and resident-memory ceiling.
- Live launch command authority requires the non-Codable activation readiness
  and resident-memory capabilities. Decoded receipts cannot recreate authority;
  the compiler rechecks exact PID/start/executable/environment/argv, kernel
  ceilings, stopped-gate sandbox identity, nil candidate cwd, and I/O paths.
- Binding plus launch append atomically as one two-event journal transaction for
  the active undisposed attempt and live productive owned process-tree lease.
  Cross-wired launches reject, and close/reopen replay retains only inert facts.
- The observation issuer now requires exactly one matching launch, its accepted
  binding, and exact natural-exit process identity before a result can resolve.
- Fourteen focused tests passed. The exact source suite passed 807 tests with 8
  intentional skips and zero failures in 59.543 test seconds. Package-owned
  807/8/0 passed in 65.538 test seconds; Release built in 105.20 seconds; signing,
  archives, hashes, exact packaged-Mach-O startup, cleanup, and read-only
  EasyBusiness checks passed.
- Strict ledger interval `external-dependency-runtime-launch-journal-219` adds
  600 accepted active seconds, bringing the exact total to 173,010 across 219
  intervals. Builds, test runtime, waits, polling, and report time count zero.
- No production external observer ran. Ordinary macOS still lacks the trusted
  hard resident-memory issuer, so production retains the typed pre-admission
  veto. Actual native spawn/output/release composition, controller invocation,
  legacy retirement, unlocked proof, clean rebuild, commit, and push remain.

## 2026-08-15 journal-owned external-dependency observation issuance

- The process-runtime-only release-bound result can now enter exactly one
  durable external-dependency observation through a dedicated journal
  coordinator; callers cannot supply the observation or reducer authority.
- The coordinator re-resolves active attempt, activation, dependency, probe,
  release transaction, and release frame, then revalidates the entire result
  authority and derives availability/evidence from the strict parse and
  declared mapping.
- Every newly issued receipt binds source result, evidence-set, release receipt,
  and release-frame provenance. Partial provenance rejects; optionality exists
  only to keep older journals available as inert forensic replay.
- Exact retry is byte/receipt exact, duplicate resolution and activation veto
  reject, forged mapping cannot enter, and close/reopen retains the exact
  observation without reconstructing live authority.
- The exact source suite passed 806 tests with 8 intentional skips and zero
  failures; package-owned 806/8/0, Release, deep-strict signing, archives,
  checksums, exact packaged-Mach-O startup, cleanup, thermal, and read-only
  EasyBusiness checks passed.
- Strict ledger interval `external-dependency-journal-observation-issuer-218`
  adds 600 accepted active seconds, bringing the exact total to 172,410 across
  218 intervals. Builds, test runtime, waits, polling, and report time count
  zero.
- Production still produces no observation on ordinary macOS: the trusted
  Release resident-memory issuer is unavailable, so launch remains vetoed.
  Native launch/output/release composition, controller invocation, legacy
  retirement, unlocked native proof, clean rebuild, commit, and push remain.

## 2026-08-15 external-dependency result authority is release-bound

- Strict parse receipts now bind the full newline-terminated standard-output digest and byte count in addition to the canonical envelope digest, eliminating a gap between retained bytes and parse evidence.
- The new result authority composes only the exact ratified activation/probe, bounded retained stdout, empty stderr, canonical parse, unique result mapping, natural process-group exit, release ownership, and one non-duplicate hash-journal release frame.
- The self-digested durable result is inert. Its live wrapper requires the file-private journaled process-runtime issuer; decoded result or release data cannot recreate it, and it still cannot append a dependency observation.
- Six focused tests passed. The exact source suite passed 805 tests with 8 intentional skips and zero failures in 61.569 test seconds.
- Current dirty-source snapshot `20ebda0a6870940079d93b21bbc345636aaa8875cc970b3e2fea24685fd879be` was packaged. The package-owned 805-test suite passed in 62.364 test seconds; Release built in 95.82 seconds; signing, archives, hashes, exact packaged-Mach-O startup, and cleanup passed.
- Trusted Release containment or the retained veto, journal-owned native launch/output/release composition and observation issuance, production-controller invocation, legacy retirement, unlocked native proof, clean rebuild, commit, and push remain pending. The strict ledger accepts 600 active seconds and totals 171,810 seconds. EasyBusiness remained read-only.

## 2026-08-15 exact external-dependency result mapping remains inert

- The executable-probe contract now recomputes the exact minimal-environment digest and requires one immutable shell-free private-request/private-output capture identity; arbitrary valid SHA-256 placeholders reject before launch.
- Canonical parse receipts now revalidate their full terminal envelope digest. Dependency, recipe, nonce, result, and evidence identity cannot be cross-wired or replaced with a different valid-looking digest.
- A deterministic result mapper binds the exact activation, ratified probe, canonical parse receipt, and native exit pair, then selects exactly one declared mapping row with its ordinal and availability. Its self-digested receipt is inert and cannot mint a journal observation.
- Five focused tests passed. The exact source suite passed 804 tests with 8 intentional skips and zero failures in 53.764 test seconds.
- Current dirty-source snapshot `137063c4f1d6c4eee61a025ba3b14d0c2c2ee0fe6f7f437e99f5da834b030437` was packaged. The package-owned 804-test suite passed in 58.864 test seconds; Release built in 96.09 seconds; signing, archives, hashes, exact packaged-Mach-O startup, and cleanup passed.
- Trusted Release resident-memory containment or the retained typed veto, journal-owned native launch/output retention/observation issuance, production-controller invocation, legacy retirement, unlocked native proof, clean rebuild, commit, and push remain pending. The strict ledger accepts 600 active seconds and totals 171,210 seconds. EasyBusiness remained read-only.

## 2026-08-15 external-dependency launch readiness fails closed

- A new production readiness coordinator now re-resolves the exact live activation transaction and revalidates both retained request and staged executable identity before resource admission.
- Native launch requires a non-Codable hard resident-memory capability bound to the exact run, activation receipt, and declared maximum. Ordinary macOS has no Release issuer for that guarantee, so the missing-capability path journals one exact typed veto before admission.
- Reducer command admission and decoded-event replay share the veto validator. Exact retry and crash replay retain causal evidence but cannot reconstruct launch authority; cross-wired containment, altered ceiling, duplicate veto, stale activation, and artifact drift fail closed.
- Tests prove the veto creates no runtime admission, lease, sandbox, process, output, or dependency observation. The exact source suite passed 803 tests with 8 intentional skips and zero failures in 61.267 test seconds.
- Current dirty-source snapshot `d33b9c3dabfb24b3d7d8d0bf16dc0cd2283b1278ad85a6193c1b7003f28ce36e` was packaged. The package-owned 803-test suite passed in 55.982 test seconds; Release built in 104.81 seconds; signing, archives, hashes, exact packaged-Mach-O startup, and cleanup passed.
- Trusted Release containment or the retained veto, actual native launch/result composition and observation issuance, production-controller invocation, legacy retirement, unlocked native proof, clean rebuild, commit, and push remain pending. The strict ledger accepts 600 active seconds and totals 170,610 seconds. EasyBusiness remained read-only.

## 2026-08-15 journaled inert external-dependency activation

- The production activation coordinator now resolves the exact active attempt, dependency, observer lineage, executable probe, and canonical workspace; stages immutable observer bytes by SHA-256; binds the exact hash-journal sequence and frame; and creates one canonical nonce request inside the owner-private journal run directory through descriptor-relative exclusive no-follow I/O.
- The request is read-only and binds complete envelope bytes, SHA-256, byte count, device, and inode. Exact retry survives a crash; tampering, writable replacement, symlink preseed, unsafe directory permissions, stale provenance, and duplicate attempt/dependency activation fail closed.
- Reducer command admission and decoded-event replay share one exact activation validator. Replay retains only the receipt and cannot recreate the live request capability.
- Activation is intentionally inert: no process, lease, native sandbox, parsed result, availability mapping, or observation authority exists yet. Four activation tests and one reducer test passed; the exact source suite passed 803 tests with 8 intentional skips and zero failures in 56.422 test seconds.
- Current dirty-source snapshot `48c4ee6d8b6af062d3109e064b328ecae5041d9bc872aa4f8784724c14c196e6` was packaged. The package-owned 803-test suite passed in 60.837 test seconds; Release built in 95.24 seconds; signing, archives, hashes, exact packaged-Mach-O startup, and cleanup passed.
- Resource admission, native launch with retained request input, bounded output/native-exit mapping, journal-owned observation issuance, controller invocation, legacy retirement, unlocked native proof, clean rebuild, commit, and push remain pending. The strict ledger accepts 600 active seconds and totals 170,010 seconds. EasyBusiness remained read-only.

## 2026-08-14 immutable external-dependency executable probe contract

- Every newly valid external dependency now carries one complete executable observation recipe: direct local transport, exact executable SHA-256, bounded shell-free argv with exactly one private request token, minimal kernel environment and exact capture identity, explicit network policy, a built-in parser implementation digest, complete `available`/`unavailable` result mappings, and positive resource ceilings with child processes forbidden. Legacy missing fields remain decodable only for forensic recovery and cannot validate or enroll.
- The strict result parser accepts exactly one newline-terminated canonical sorted-key JSON object with six keys. Dependency, recipe, fresh request nonce, result code, schema, and evidence SHA-256 are all exact; extra keys/lines, CR bytes, stale identities, noncanonical encoding, unsupported results, invalid UTF-8, and malformed digests fail closed. Its non-Codable parse authority is inert and cannot issue a reducer observation.
- Reducer observation admission now requires an exact lowercase SHA-256 evidence digest. Four focused parser/contract tests and 75 affected tests passed. The exact accepted source suite passed 798 tests with 8 intentional environment skips and zero failures in 64.520 test seconds.
- Dirty-source snapshot `c4667031cc136e0a780198f21053b79ea07f02760d9790933b7fc1bc6e2d3e30` was packaged. The package-owned 798-test suite passed in 1012.583 test seconds; its dependent-worktree integration test passed in an observed 958.115 seconds, while the real Codex child/Responses bridge test separately passed in 9.908 seconds. Release built in 114.67 seconds; signing, ZIP, DMG, checksums, manifest binding, exact packaged-Mach-O startup, and cleanup passed.
- This is a schema and parse-authority prerequisite, not a production observer. Descriptor-backed nonce request materialization, exact executable staging, resource lease and native sandbox launch, native exit/mapping composition, journal-owned observation issuance, controller invocation, unlocked screenshots, legacy retirement, clean rebuild, commit, and push remain pending. The strict ledger accepts 600 active seconds and totals 169,410 seconds. EasyBusiness remained read-only.

## 2026-08-14 external-dependency replay authority and current package

- External-dependency observation command admission and decoded-event replay now share one exact pure validator over active attempt, disposition, declared contract, requirement intersection, authorized independent observer lineage, evidence recipe, nonempty digest, and accepted event time; command admission also binds the observer to its actor.
- Replay rejects every duplicate receipt ID before state mutation. A tampered decoded event cannot bypass command validation or replace an accepted unavailable observation with available evidence under the same identity.
- Two adversarial replay tests joined the reducer suite. The focused reducer suite passed 42 tests; the exact full source suite passed 794 tests with 8 intentional environment skips and zero failures in 56.074 test seconds.
- The current dirty-source snapshot `ad26dc732c706074efa5ef1f17eec65431a5d4a448e6fce2d30b741b1bb162db` was packaged. Its package-owned 794-test suite passed in 60.582 test seconds; Release built in 102.12 seconds; ad-hoc deep-strict signature, ZIP, DMG, hashes, exact packaged-Mach-O startup, and cleanup passed.
- This closes replay authority, not observation execution. A ratified executable probe schema, production observer launch/composition, controller invocation, unlocked native screenshots, clean-commit rebuild, commit, and push remain pending. The strict ledger accepts 600 active seconds and totals 168,810 seconds. EasyBusiness remained read-only.

## 2026-08-14 journaled kernel final-completion authority

- Final completion now has one deterministic production issuer that executes no model and trusts no completion prose. It resolves the exact completion-requested reducer state, SHA-256 journal head, and latest accepted event time, then compiles one evidence digest over all typed closure facts.
- Mandatory requirements, ratified duration, terminal runtime quiescence, authorized and available external-dependency observations, latest native green visual evidence, safe integration state, independent integration acceptance for mutation-backed requirements, and distinct actor lineages are hard vetoes. One red or missing fact advances nothing.
- The hash journal atomically retains the exact authorization receipt before the completed transition. Replay recomputes the evidence against the preceding state, exact retry returns the retained two-event transaction, command conflicts fail closed, tampering leaves state unchanged, and legacy completion is not silently promoted.
- Five focused completion tests passed. The later external-dependency replay-hardening snapshot supersedes this section's original 792-test source receipt and now passes 794 tests plus current dirty-source package/sign/hash/smoke. The strict ledger for this completion-authority interval accepted 600 active seconds and totaled 168,210 seconds.
- Production-controller invocation, executable external-dependency observation, trusted Release containment or retained veto, legacy cutover, unlocked native proof, clean-commit rebuild, commit, and push remain pending. EasyBusiness remained read-only.

## 2026-08-12 inert pre-apply candidate isolation

- Production-enrollment-bound isolation now creates one owner-private writable candidate beneath the exact journal run directory and copies only the complete user-ratified source revision through descriptor-relative no-follow reads, bounded byte hashing, exclusive creation, sync, atomic install, and independent tree verification.
- The self-digested receipt binds run, contract, attempt, node, strategy, source/policy/enrollment frame, physical root identity, manifest, file/byte totals, and time. Its non-Codable capability holds the exact root descriptor; changed bytes, source drift, closed authority, decoded receipt tampering, and path replacement fail closed.
- Issuance is intentionally inert: it appends no journal frame, starts no attempt/process, leaves the reducer state at enrollment, and leaves both mutation blockers present. Journal acceptance, descriptor-retaining candidate-root activation, post-worker logical candidate revision, and the acyclic delta/content/manifest/preflight chain remain required.
- Exact final-source coverage is 756 unique tests with 8 intentional environment skips and 0 failures. The process-group resistance case passed again independently but is not double-counted. Release builds and exact hashes passed. The strict ledger accepts 600 active seconds and now totals 155,970 seconds; two failed compile diagnostics, one failed test-compilation diagnostic, all tests/builds, waits, polling, cleanup checks, and reporting count zero.
- No EasyBusiness mutation, native retry, package/sign, commit, or push occurred.

## 2026-08-12 pre-apply candidate authority veto

- The exact candidate-byte receipt is structurally post-apply, while integration apply requires an accepted preflight. Reusing that receipt to assemble preflight would be circular authority.
- Mutation-capable readiness had nevertheless reported no blocker and production activation still aimed its writable sandbox at the canonical workspace.
- Typed plan/contract projections now retain explicit pre-apply isolation and journaled preparation blockers for mutation-capable contracts. Production composition rejects before preparation and the lower activation boundary rejects before `startAttempt`; the journal remains unchanged. Exact read-only contracts remain eligible.
- Split current-source coverage is 752 tests plus the independent resistance case, for 753 covered, 8 intentional environment skips, and 0 failures. Release builds and exact source hashes passed. The strict ledger accepts 600 active seconds and now totals 155,370 seconds; the failed fixture diagnostic, all tests/builds, waits, polling, and reporting count zero.
- No candidate isolation, manifest, preflight, effect, publication, native start, package, commit, or push authority was issued.

## 2026-08-12 ratified baseline capture journal acceptance

- The live non-Codable baseline-content capability now has one exact production journal ingress. Admission rehashes its bytes and validates run, contract, ratification, enrollment frame, workspace, root, actor, time, derivation, base revision, policy, and source-revision membership before a receipt-only event can enter the hash chain.
- Replay reconstructs a reducer-owned capture projection but cannot recreate the live bytes or mutation authority. One derivation admits one capture; duplicate command IDs resolve only to the exact retained receipt, while corrupt bytes and different captures fail without advancing the journal.
- Candidate origin, complete before/after composition, store acceptance, HEAD/index/untracked authority, preimage/manifest assembly, preflight, resident memory, publication, native baseline/visual/final authority, native start, package/sign, commit, and push remain absent or blocked.
- Split current-source coverage is 744 tests plus the independent resistance case, for 745 covered, 8 intentional environment skips, and 0 failures. Release builds and exact source hashes passed. The strict ledger accepts 600 active seconds and now totals 151,770 seconds; failed diagnostic/compile attempts, tests, builds, waits, polling, and reporting count zero.

## Confirmed observations at intake

1. The user-observed UI failure is first-order, not cosmetic polish: typography, icon/shape geometry, density, spacing, hierarchy, and the overall visual language diverged far enough from the Chinese product that feature-level evaluation is no longer meaningful.
2. A seven-iteration failure did not cause the Graph to retire the strategy or abandon the node. This is direct evidence of a convergence-policy defect, not merely weak prompting.
3. Ending the task preserved eight category-node files as an uncommitted candidate in the canonical EasyBusiness workspace. A stopped task therefore does not guarantee a clean, attributable integration boundary.
4. The earlier run counted agent activity while visible product quality regressed. Runtime/iteration/test completion metrics were acting as misleading proxies for product quality.
5. Prior thermal evidence captured a transient LoopForge CPU spike in SwiftUI/AttributeGraph while `GraphLoopMap` recomputed the full DAG layout after transient publication; the old sample is `/tmp/loopforge-build133-cpu-spike-20260809-2103.sample.txt`.
6. The EasyBusiness backend process PID 50060 predates this Graph run by days, is parented by launchd, consumes 0% CPU at intake, and is preserved as an unrelated user service.

## Initial systemic hypotheses to prove or falsify

- The Graph objective allows functional/localization edits to overwrite product identity because it lacks an immutable design baseline and measurable visual invariants.
- Main review and Node generation share too much context and too few independent signals, producing self-confirming approval rather than adversarial review.
- Repeated failure increments iterations but does not force a materially different strategy, scope reduction, node retirement, or escalation.
- Mutation and integration are not transactionally isolated from the canonical workspace across pause/end/crash boundaries.
- Tests disproportionately reward text presence, accessibility identifiers, and runtime success while omitting typography, icon geometry, layout density, hierarchy, and before/after screenshot gates.
- Timer and progress semantics encourage accumulating work rather than minimizing destructive change or demonstrating convergence.
- Process cleanup, render invalidation, and resource ownership are insufficiently coupled to the task lifecycle.

These are hypotheses only. Each must be linked to exact logs, plans, code paths, patches, commits, screenshots, and reproductions before it is promoted to a root cause.

## 2026-08-12 ratified baseline byte-origin boundary

- The durable mutation object store previously proved retained byte equality but accepted its source bytes from the caller.
- Production enrollment already provided the missing baseline authority chain: the recovery registry binds one ratification receipt and enrollment journal frame to one canonical workspace, while journal replay retains the exact user-confirmed source revision.
- `WorkspaceRatifiedBaselineContentCaptureIssuer` now requires that complete chain, recaptures the baseline around descriptor-stable reads, and returns only a non-Codable capability containing exact expected-preimage bytes.
- This does **not** authorize candidate bytes. A post-apply candidate attestation cannot be repurposed as a preflight source, and no current pre-apply candidate-isolation capability is journal-owned. Complete object-set assembly and store journaling therefore remain blocked rather than inferred.
- Split current-source coverage is 741 tests plus the independent resistance case, for 742 covered, 8 intentional environment skips, and 0 failures. Release builds and exact source hashes passed. The strict ledger accepts 600 active seconds and now totals 151,170 seconds; failed diagnostic/compile attempts, tests, builds, waits, polling, and reporting count zero.
# 2026-08-12T11:56:27Z — Pre-apply isolation receipt enters the pure reducer/hash journal

- Implemented the next exact mutation cutover boundary: `RunJournal` now
  accepts only the live non-Codable pre-apply isolation capability after
  immediate descriptor/tree/path revalidation, then persists one inert,
  self-digested receipt event.
- The durable receipt now includes the production enrollment actor. The pure
  reducer independently binds run, active contract, mutation-capable retained
  plan/node/strategy, exact source revision and capture policy, workspace/root,
  enrollment frame, actor, and time. One receipt is retained per attempt.
- Duplicate delivery revalidates the live candidate before returning retained
  evidence. A mutated, replaced, or closed candidate cannot hide behind an old
  command ID; a different attempt/candidate using that ID conflicts with no
  journal advance.
- Replay performs no filesystem I/O and reconstructs only the receipt. Journal
  serialization contains neither the root descriptor nor candidate bytes.
- Acceptance advances exactly one event while leaving phase `ready`, nodes and
  attempts empty, both mutation blockers uncleared, and production activation
  vetoed. The original live descriptor still has no activation consumer.
- Three new adversarial tests cover receipt-only recovery/inertness, live
  pristine duplicate validation, and exact command-ID conflict. The focused
  isolation family passed 6 tests. The complete exact-source suite passed 759
  tests with 8 intentional environment skips and zero failures.
- Release build passed. Exact hashes: LoopForge
  `bb3a011ffacb9d9af3cc87eb860410519101c79ac7251f65281b65dfe0208d84`;
  KernelSandboxGate
  `1b7ba433fe915b0dd916aceafe62342d246bcf6bbddcfc5425b0d09fba0fbeaa`;
  pre-report source snapshot
  `661566da74ebcd051e1b7be1b2d3de1772cfee9507f953a6ffddf94f7c21f82c`.
- Strict ledger interval
  `kernel-preapply-candidate-journal-acceptance-191` adds 600 accepted active
  seconds, bringing the exact total to 156,570 across 191 intervals. The one
  failed test-compilation diagnostic, all tests/builds, polling, waits, cleanup
  checks, and report work count zero.
- EasyBusiness remained read-only with the exact unchanged seven modified and
  one untracked path fingerprint. No LoopForge-owned test child remained.
- Still blocked: descriptor-retaining candidate-root activation; post-worker
  logical candidate capture; acyclic delta/content-store/manifest/preparation/
  rollback/proposal/preflight; resident-memory authority; native design/visual/
  final authority; native start; current package/sign; unlocked UI proof; clean
  commit; push.

# 2026-08-12T15:07:14Z — Production activation consumes and retains the candidate descriptor

- Mutation activation now requires the original accepted live isolation
  capability, consumes it exactly once before preparation, and retains an
  independently owned root descriptor in the production execution session.
- The execution proof, session receipt, provider invocation receipt,
  workspace-only sandbox, and process launch all bind the same exact isolation
  receipt. Immediately before spawn, the complete pristine tree and pathname
  identity are revalidated through the descriptor.
- The process adapter uses descriptor-relative Darwin spawn `fchdir`; the
  stopped native sandbox gate must attest the candidate root's exact device and
  inode before the worker resumes.
- Candidate storage moved from beneath journal evidence to an owner-private
  sibling execution tree. This retires the contradictory state where the
  workspace-only sandbox had to grant writes beneath the hash journal.
- Production completion now derives the enriched post-binding lease from the
  exact provider launch transaction rather than replaying the pre-binding
  admission lease.
- A real production-session `KernelProcessFixture` launch, cwd attestation,
  journaled natural release, invalid terminal-result rejection, and zero-live/
  zero-in-doubt cleanup passed. Substituted and stale capability tests reject
  before start and preserve the journal.
- The process-group resistance test's unrelated `/bin/sleep` cleanup is now
  bounded and escalates only its exact spawned PID, retiring an observed
  unbounded `waitUntilExit` suite hang.
- The complete affected surface passed 97 tests with no skips or failures. The
  complete Swift suite passed 761 tests with 8 intentional environment skips
  and zero failures.
- Strict ledger interval `kernel-preapply-candidate-activation-192` adds 600
  accepted active seconds, bringing the exact total to 157,170 across 192
  intervals. Failed compilations, failed launch diagnostics, the timed-out
  release attempt, the interrupted hanging suite, all tests/builds, waits,
  polling, cleanup checks, and report work count zero.
- EasyBusiness remained read-only with the exact unchanged seven modified and
  one untracked path fingerprint. No LoopForge-owned test child remained.
- Still blocked: post-worker logical candidate capture and the acyclic delta/
  content-store/manifest/preparation/rollback/proposal/preflight chain;
  resident-memory authority; native design/visual/final authority; native UI;
  current package/sign; clean commit; push.

# 2026-08-12T15:51:40Z — Completed candidate bytes and provenance are journaled

- The production session now captures only after the exact journaled release,
  terminal parse, and reducer-derived execution report `completed`, with no
  live, queued, failed, or in-doubt runtime resource.
- Capture walks the retained candidate descriptor with no-follow stable reads,
  ratified exclusions, bounded bytes, and a second independent enumeration and
  full rehash. The canonical EasyBusiness workspace is never opened for write.
- The receipt preserves the ratified logical workspace ID/root while recording
  the physical candidate root, device, inode, and manifest separately. It binds
  exact release/parse/execution receipt IDs and journal frame digests.
- A non-Codable capability retains all deduplicated immutable bytes. Crash
  replay restores the full logical revision and physical/provenance receipt but
  intentionally cannot reconstruct byte authority from a path.
- Exact mutation operations derive only from the prepared node's requirement
  IDs and writable paths, and every desired postimage must exist in the retained
  object set. One completion receives one immutable capture attempt.
- Descriptor capture/mismatch coverage and enrolled journal close/reopen replay
  passed. The complete Swift suite passed 764 tests with 8 intentional
  environment skips and zero failures.
- Strict ledger interval `kernel-completed-candidate-capture-193` adds 600
  accepted active seconds, bringing the exact total to 157,770 across 193
  intervals. Failed compile/test diagnostics, one transient existing timeout,
  all tests/builds, waits, polling, cleanup checks, and report work count zero.
- EasyBusiness remained read-only with the exact unchanged seven modified and
  one untracked path fingerprint. No LoopForge-owned test child remained and
  the host reported no thermal or performance warning.
- Still blocked: production content-store composition and the remaining
  manifest/preparation-facts/rollback/proposal/preflight authority chain;
  resident-memory authority; native design/visual/final authority; native UI;
  current package/sign/hash; clean commit; push.

# 2026-08-12T16:16:32Z — Completed bytes enter a distinct preflight-side store

- Exact journal-accepted ratified baseline bytes and completed-candidate bytes
  now compose into one independently rehashed before/after object set before
  any integration apply event exists. The circular post-apply path is not
  reused.
- The sealed content-addressed artifact lives outside the canonical workspace.
  A substituted capture transaction rejects before store creation, while exact
  duplicate replay remains idempotent and still revalidates the artifact.
- The pure reducer independently binds the completed attempt and node, exact
  contract/source/workspace, requirement ownership, writable-path containment,
  before/after partitions, capture journal frame, actor, and time.
- RunJournal revalidates both non-Codable live byte origins and the external
  artifact before append. Crash replay restores only the self-digested receipt.
- The affected content/integration/enrollment surface passed 52 tests. The
  complete Swift suite passed 764 tests with 8 intentional environment skips
  and zero failures.
- Strict ledger interval `kernel-completed-candidate-content-store-194` adds
  600 accepted active seconds, bringing the exact total to 158,370 across 194
  intervals. The initial compile diagnostic, one cleanup-permission test
  diagnostic, all tests/builds, waits, polling, cleanup checks, and report work
  count zero.
- EasyBusiness remained read-only with the unchanged seven modified and one
  untracked path fingerprint. No LoopForge-owned test child remained and the
  host reported no thermal or performance warning.
- Still blocked: acyclic manifest/preparation-facts/rollback/proposal/preflight
  authority; resident-memory authority; native design/visual/final authority;
  native UI; current package/sign/hash; clean commit; push.

# 2026-08-12T16:52:43Z — Inert mutation-manifest proposal is journaled

- The exact accepted completed-candidate content store and exact accepted
  canonical preimage now compose into one self-digested, crash-replayable
  mutation-manifest proposal bound to the active contract, canonical plan node,
  completed attempt/strategy, exact operation topology, requirement ownership,
  file/byte budgets, and common preparation binding.
- The proposal operation type cannot carry a path-resolution receipt. Serialized
  evidence contains no path, write, mutation-budget, verification, independent-
  review, rollback, quiescence, or visual authority, creates no integration
  transaction, and cannot invoke canonical workspace effects.
- A substituted canonical-preimage transaction rejected; exact duplicate append
  remained idempotent; journal close/reopen recovered only inert receipt
  evidence; the canonical fixture remained byte-identical.
- Nine native XCTest crash diagnostics proved one cooperative-thread stack-
  overflow pattern in Foundation validated-format SHA byte encoding. A bounded
  locale-free `KernelHex` encoder now covers the affected verifier, provider,
  cache, compiler/parser, and proposal digest paths. Failed diagnostics count
  zero.
- The complete Swift suite passed 764 tests with 8 intentional environment
  skips and zero failures. Diff whitespace, owned-process cleanup, thermal
  checks, and the unchanged read-only EasyBusiness fingerprint passed.
- Strict ledger interval `kernel-completed-candidate-manifest-proposal-195` adds
  600 accepted active seconds, bringing the exact total to 158,970 across 195
  intervals. Failed crash diagnostics, all failed runs, tests/builds, waits,
  polling, cleanup checks, and report work count zero.
- Still blocked: exact preparation facts, rollback rehearsal, executable
  manifest assembly, proposal/preflight authority, resident-memory authority,
  native design/visual/final authority, native UI, current package/sign/hash,
  clean commit, and push.

# 2026-08-12T20:43:00Z — Descriptor-observed preparation facts are journaled

- The exact accepted inert manifest proposal now yields four separately typed,
  common-bound historical facts: least affected-path write scope, exact
  observed file/byte ceilings, candidate-scoped quiescence bound to the exact
  successful release frame, and descriptor-observed path resolution.
- Canonical root components and affected path components are opened with
  `O_NOFOLLOW`; stable root/file inode, mode, size, and preimage content are
  checked. Create targets must be absent below an existing no-follow parent.
  Duplicate replay repeats this observation and rejects current drift.
- A substituted proposal frame and a symlinked canonical target both rejected.
  After the synthetic fixture was restored, exact preparation, duplicate
  idempotency, and close/reopen receipt replay passed without integration or
  canonical mutation. Serialized facts retain no rollback, verification,
  independent-review, or visual authority.
- Seven failed signal-10 validation runs reproduced one cooperative-thread
  stack-guard overflow whose incidental top frame moved through metadata, URL,
  `realpath`, and variadic digest helpers. They count zero. The remaining
  verifier-result byte formatting now uses bounded `KernelHex`, and the exact
  release predicates were extracted unchanged from the oversized reducer
  command switch into a dedicated validator.
- The previously crashing integration test passed, then the complete Swift
  suite passed 764 tests with 8 intentional environment skips and zero
  failures in 62.809 test seconds. Diff whitespace, owned-process cleanup,
  thermal checks, and the unchanged read-only EasyBusiness fingerprint passed.
- Strict ledger interval `kernel-completed-candidate-preparation-facts-196`
  adds 600 accepted active seconds, bringing the exact total to 159,570 across
  196 intervals. Failed runs, tests/builds, waits, polling, cleanup checks, and
  report generation count zero.
- Still blocked: rollback rehearsal, executable manifest assembly and preflight
  authority; resident-memory authority; native design/visual/final authority;
  native UI; current package/sign/hash; clean commit; push.
